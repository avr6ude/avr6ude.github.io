---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<hr class:list={["divider", className]} />

<style>
  .divider {
    border: none;
    border-top: 1px solid var(--color-border);
    margin: var(--space-xl) 0;
  }
</style>
